#pragma once

#include <iostream>
#include <memory>

#include <gtkmm-4.0/gtkmm.h>
#include <glibmm-2.68/glibmm.h>

// class app_window;
// using app_window_ptr =  ;

class app_window : public Gtk::ApplicationWindow
{
public:
    app_window(BaseObjectType* cobject, const Glib::RefPtr<Gtk::Builder> &builder);

    // static app_window_ptr create();
    static std::unique_ptr<app_window> create();
};

